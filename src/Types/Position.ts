// Interface defining a Position object
export interface Position {
  left: number; // The horizontal position (distance from the left of the container)
  top: number;  // The vertical position (distance from the top of the container)
}
