// Interface defining an Egg object
export interface Egg {
  id: string;   // Unique identifier for the egg
  left: number; // The horizontal position of the egg (distance from the left of the container)
  top: number;  // The vertical position of the egg (distance from the top of the container)
}
